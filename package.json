{
  "name": "ai-clinical-triage-assistant",
  "private": true,
  "workspaces": [
    "packages/frontend",
    "packages/backend"
  ],
  "scripts": {
    "dev": "concurrently -n frontend,backend -c blue,green \"npm run dev -w packages/frontend\" \"npm run dev -w packages/backend\"",
    "build": "npm run build -w packages/backend && npm run build -w packages/frontend",
    "start": "npm run start -w packages/backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.5.4"
  }
}
